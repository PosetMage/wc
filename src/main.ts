// src/main.ts

// Import for side-effects: each Svelte file, compiled with
// customElement: true, will register itself under its <svelte:options> tag.
import './components/HelloWorld.svelte';
import './components/FancyPanel.svelte';
